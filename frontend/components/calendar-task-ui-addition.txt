Replace this section (around line 294-337):

OLD:
                {getTasksForWeek(selectedWeek).length === 0 ? (
                  <div className="text-center py-8 text-gray-500">
                    <p className="text-lg">No maintenance scheduled for this week</p>
                    <p className="text-sm mt-2">Click "Generate Schedule" to auto-populate</p>
                  </div>
                ) : (
                  ... task list ...
                )}

NEW:
                <div className="mb-4 flex justify-between items-center">
                  <p className="text-sm text-gray-600">{getTasksForWeek(selectedWeek).length} task(s) scheduled</p>
                  <button
                    onClick={() => setShowAddTask(!showAddTask)}
                    className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 font-semibold transition"
                  >
                    {showAddTask ? '✕ Cancel' : '+ Add Task'}
                  </button>
                </div>

                {showAddTask && (
                  <div className="mb-6 bg-white border-2 border-blue-400 rounded-lg p-4">
                    <h4 className="font-bold text-gray-900 mb-3">Add Manual Task for Week {selectedWeek}</h4>
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
                      <div>
                        <label className="block text-sm font-semibold text-gray-700 mb-1">Component Part</label>
                        <select
                          value={newTask.partId}
                          onChange={(e) => setNewTask({ ...newTask, partId: e.target.value })}
                          className="w-full px-3 py-2 border-2 border-gray-300 rounded-lg"
                        >
                          <option value="">Select Part...</option>
                          {parts.map(part => (
                            <option key={part.id} value={part.id}>
                              {part.partNumber} - {part.partName}
                            </option>
                          ))}
                        </select>
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-700 mb-1">Maintenance Type</label>
                        <select
                          value={newTask.maintenanceType}
                          onChange={(e) => setNewTask({ ...newTask, maintenanceType: e.target.value as any })}
                          className="w-full px-3 py-2 border-2 border-gray-300 rounded-lg"
                        >
                          <option value="PM">PM - Preventive Maintenance</option>
                          <option value="AM">AM - Autonomous Maintenance</option>
                          <option value="QM">QM - Quality Maintenance</option>
                          <option value="GM">GM - General Maintenance</option>
                        </select>
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-700 mb-1">Status</label>
                        <select
                          value={newTask.status}
                          onChange={(e) => setNewTask({ ...newTask, status: e.target.value as any })}
                          className="w-full px-3 py-2 border-2 border-gray-300 rounded-lg"
                        >
                          <option value="planned">Planned</option>
                          <option value="completed">Completed</option>
                          <option value="overdue">Overdue</option>
                        </select>
                      </div>
                    </div>
                    <div className="mt-3 flex justify-end">
                      <button
                        onClick={addManualTask}
                        disabled={!newTask.partId || loading}
                        className="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 font-semibold transition disabled:bg-gray-300"
                      >
                        ✓ Add Task
                      </button>
                    </div>
                  </div>
                )}

                {getTasksForWeek(selectedWeek).length === 0 && !showAddTask ? (
                  <div className="text-center py-8 text-gray-500">
                    <p className="text-lg">No maintenance scheduled for this week</p>
                    <p className="text-sm mt-2">Click "+ Add Task" to manually add</p>
                  </div>
                ) : (
                  <div className="space-y-3">
                    {getTasksForWeek(selectedWeek).map((task) => (
                      <div key={task.id} className="bg-white border-2 border-gray-300 rounded-lg p-4">
                        <div className="flex items-center justify-between">
                          <div className="flex items-center gap-3">
                            <span className={`${getTypeColor(task.maintenanceType)} text-white px-3 py-1 rounded-lg font-bold text-sm`}>
                              {task.maintenanceType}
                            </span>
                            <div>
                              <p className="font-bold text-gray-900">{task.partName}</p>
                              <p className="text-sm text-gray-600">Part: {task.partNumber}</p>
                            </div>
                          </div>
                          <div className="flex items-center gap-2">
                            <button
                              onClick={() => toggleTaskStatus(task)}
                              className={`px-4 py-2 rounded-lg font-semibold ${
                                task.status === 'completed' ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-700'
                              }`}
                            >
                              {task.status === 'completed' ? '✓ Completed' : 'Mark Complete'}
                            </button>
                            <button
                              onClick={() => deleteTask(task.id)}
                              className="px-3 py-2 bg-red-100 text-red-700 rounded-lg hover:bg-red-200"
                            >
                              🗑️
                            </button>
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                )}
